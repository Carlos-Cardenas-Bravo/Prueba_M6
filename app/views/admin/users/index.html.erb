<h2 class="title is-4">Lista de Usuarios Normales</h2>

<table class="table is-fullwidth is-striped is-hoverable">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Email</th>
      <th>Acciones</th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= user.email %></td>
        <td>
          <div class="buttons">
            <%= link_to 'Editar', edit_admin_user_path(user), class: 'button is-small is-link' %>
            <%= button_to 'Eliminar', admin_user_path(user), method: :delete, data: { confirm: '¿Estás seguro?' }, class: 'button is-small is-danger' %>
          </div>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
